CREATE TABLE matches (
  game_date DATE NOT NULL,
  game_mode VARCHAR NOT NULL,
  duration VARCHAR NOT NULL,
  role VARCHAR,
  champ VARCHAR NOT NULL,
  k VARCHAR NOT NULL,
  d VARCHAR NOT NULL,
  a VARCHAR NOT NULL,
  cs VARCHAR NOT NULL,
  dmg VARCHAR NOT NULL,
  shield VARCHAR NOT NULL,
  heal VARCHAR NOT NULL,
  gold VARCHAR NOT NULL,
  vision VARCHAR,
  result VARCHAR NOT NULL,
  score VARCHAR NOT NULL,
  rchamp VARCHAR,
  rk VARCHAR,
  rd VARCHAR,
  ra VARCHAR,
  rcs VARCHAR,
  rgold VARCHAR
);